#this file will start the buses and all processes

    # these will include the writers, busses, and any other system processes
    # it'll also set up any multiprocessing managers

# a reminder ipc publisher and subsribers as well as shared memory will be indexed using
# the global config file and can I can just have a blocking call on named 
# shared memory